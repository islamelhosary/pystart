def calculate_midichlorians(base_count: int, force_multiplier: int) -> int:
    """Calculate total midichlorian count for a Force-sensitive individual."""
    return base_count * force_multiplier


def greet_padawan(name: str) -> str:
    """Greet a young Padawan learner."""
    return f"May the Force be with you, {name}!"
